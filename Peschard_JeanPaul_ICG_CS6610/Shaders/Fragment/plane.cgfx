#version 330 core

out vec4 FragColor;

uniform sampler2D samplerPlane; 

uniform float screenWidth;
uniform float screenHeight;

void main()
{
	vec2 uv = vec2(gl_FragCoord.x/screenWidth,gl_FragCoord.y/screenHeight);
	FragColor = texture(samplerPlane, uv);
}